fun bool main() = let arrint = {True, False, True} in
                 let b = map(op not, arrint) in
                 let asds = write(length(b)) in let as = write(b[0]) in let asv =write(b[1]) in write(b[2])
